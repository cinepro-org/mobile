/** Matches native splash + dark theme `colors.ink` — avoids launch flicker. */
export const SPLASH_BACKGROUND = '#07080d';

/** Minimum time to show the JS Reanimated splash after the native overlay is dismissed. */
export const SPLASH_MIN_DURATION_MS = 2400;
